# =========================================================================
# КОНФИГУРАЦИЯ СБОРКИ И ДЕПЛОЯ NETLIFY
# Запускает build-env.js перед публикацией статического приложения
# =========================================================================
[build]
  command = "node build-env.js"
  publish = "."
